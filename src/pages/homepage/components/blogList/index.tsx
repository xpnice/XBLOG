import BlogCard from "../blogCard"
const BlogList: React.FunctionComponent = () => {
    return (<div>
        <BlogCard info={null} />
        <BlogCard info={null} />
    </div>
    )
}
export default BlogList